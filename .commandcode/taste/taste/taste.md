# Taste
- Communicates in Simplified Chinese; expects responses in Chinese. Confidence: 0.9
- Session onboarding follows an explicit sequenced routine the user dictates step-by-step ("先…然后…"): (1) analyze/understand the current project state, (2) read and cross-check logs/records, (3) then read the specified plan document before any implementation. Follow the given order. Confidence: 0.7
- Values cross-validation of records against reality (progress log ↔ git commits ↔ issue tracker) before reporting status, and expects unverified items to be flagged honestly (e.g., "pending user confirmation" not treated as done). Confidence: 0.7
- Plan-driven phased execution: after the plan doc is reviewed, the user steers with terse phase go-aheads (e.g. "按 P2（音效反馈）或 P3（功能画面）继续推进") and expects the agent to pick the plan's next phase, work through it autonomously (todo list, code, docs), and check off completed items in the plan doc. Confidence: 0.8
- Yuris-Tyn methodology: evidence-level driven reverse engineering — classify every finding as Confirmed / Likely / Hypothesis / Unknown; never guess unverified semantics (record Unknown with the reason, e.g. "dynamic-dispatch writer not reversed"); upgrade Likely → Confirmed only after the user's on-machine confirmation. Confidence: 0.9
- Forensic/reverse-engineering probes are written as committed cargo examples (`tmp_*.rs` under the relevant crate's `examples/`, with doc-comment + usage line) so evidence stays reproducible; methodology pitfalls discovered while analyzing (e.g. byte-scan counts polluted by overlapping pool windows) get recorded in the progress log. Confidence: 0.8
- Definition of done for an implementation task: regression tests for new behavior (minimal synthetic fixtures, assert on observable decision logs rather than hardware/audio output) → `cargo test -p <crate>` → `cargo test --workspace` → `cargo build --release` → update the plan doc and PROGRESS.md (append a numbered 成果 entry with 取证/实现/验证/待确认 sections; when real-machine verification invalidates an earlier implementation, append numbered 勘误 entries recording what was wrong and the re-derived evidence instead of silently rewriting — e.g. 勘误 2 for the falsified self-invented EXTRA landing menu). Confidence: 0.85
- On-machine verification is performed by the user, and the handoff must be executable: when a change needs visual/audio/manual confirmation, provide the exact run command (e.g. `cargo run --release -p yuris-cli -- run "<game dir>"`) together with concrete expected checkpoints (what sound/behavior/log lines to look for), not just a "please verify" note. User follows this loop (ran the P2 handoff and reported back "测试正常"; ran the P3 handoff and reported back tersely from the real machine, "界面能打开，但是UI没有"; after the EXTRA rework reported once more from the real machine, "CG 鉴赏屏的UI不完整和有些错位" — always terse, text-only defect reports, no screenshots). Confidence: 0.9
- Git commits are managed by the user, not the agent: the user committed the completed P2 phase themselves between sessions (`6e49abd`), and the agent finishes each phase with working-tree changes left uncommitted, offering to commit only on request ("需要提交说一声即可"). Confidence: 0.6
s/saveload/back_load`, not `saveload/back_load`) — `resolve_entry` exact-matches the full path and fuzzy-matches only within the same directory, so shortened paths silently miss (real-machine symptom: sub-screen opens but no UI drawn). Verify actual entry names/suffixes from probe output before wiring (e.g. bt3-series tab buttons are `btn_tab_*_bt3n`, no bare-name entry), and treat the defensive "素材未命中" eprintln lines as the runtime signal for misses. Confidence: 0.8
.8
 the repo) to learn layout semantics before writing any layout. The self-invented EXTRA landing menu caused the real-machine bug "UI重复错位、背景是透明黑"; native scripts showed EXTRA goes straight to the CG gallery (no landing menu exists), `cgmode/back` is a 3×3 grid (9/page), and bt3-series buttons are horizontal state atlases cropped per segment (`load_atlas_segment`). Native elements not yet implementable get explicit 裁剪 records, never placeholder buttons. Confidence: 0.85
t its feature is unimplemented, render it in its native disabled state (bt3n atlas 4th `_na` dark segment for the RP/MV tabs) rather than omitting it — omission reads as broken, disabled-native reads as faithful. Only truly missing assets get 裁剪 records. Confidence: 0.7
- "错位" (misalignment) reports are fixed by re-measuring the extracted asset geometry (e.g. cgmode/back white-grid column/row origins, atlas segment pitch) and re-anchoring thumbnails pixel-precisely with a small inner inset — not by nudging offsets; spacing that remains unmeasured is marked (Likely) in code and docs. Confidence: 0.7
