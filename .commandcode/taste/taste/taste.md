# Taste
- Communicates in Simplified Chinese; expects responses in Chinese. Confidence: 0.9
- Session onboarding follows an explicit sequenced routine the user dictates step-by-step ("先…然后…"): (1) analyze/understand the current project state, (2) read and cross-check logs/records, (3) then read the specified plan document before any implementation. Follow the given order. Confidence: 0.7
- Values cross-validation of records against reality (progress log ↔ git commits ↔ issue tracker) before reporting status, and expects unverified items to be flagged honestly (e.g., "pending user confirmation" not treated as done). Confidence: 0.7
- Plan-driven phased execution: after the plan doc is reviewed, the user steers with terse phase go-aheads (e.g. "按 P2（音效反馈）或 P3（功能画面）继续推进") and expects the agent to pick the plan's next phase, work through it autonomously (todo list, code, docs), and check off completed items in the plan doc. Confidence: 0.8
- Yuris-Tyn methodology: evidence-level driven reverse engineering — classify every finding as Confirmed / Likely / Hypothesis / Unknown; never guess unverified semantics (record Unknown with the reason, e.g. "dynamic-dispatch writer not reversed"); upgrade Likely → Confirmed only after the user's on-machine confirmation. Confidence: 0.9
- Forensic/reverse-engineering probes are written as committed cargo examples (`tmp_*.rs` under the relevant crate's `examples/`, with doc-comment + usage line) so evidence stays reproducible; methodology pitfalls discovered while analyzing (e.g. byte-scan counts polluted by overlapping pool windows) get recorded in the progress log. Confidence: 0.8
- Definition of done for an implementation task: regression tests for new behavior (minimal synthetic fixtures, assert on observable decision logs rather than hardware/audio output) → `cargo test -p <crate>` → `cargo test --workspace` → `cargo build --release` → update the plan doc and PROGRESS.md (append a numbered 成果 entry with 取证/实现/验证/待确认 sections). Confidence: 0.8
- On-machine verification is performed by the user, and the handoff must be executable: when a change needs visual/audio/manual confirmation, provide the exact run command (e.g. `cargo run --release -p yuris-cli -- run "<game dir>"`) together with concrete expected checkpoints (what sound/behavior/log lines to look for), not just a "please verify" note. Confidence: 0.7
